--- app.html	2026-06-26 17:28:52.758025076 +0000
+++ app.modified.html	2026-06-26 17:30:42.145300243 +0000
@@ -1539,7 +1539,7 @@
         </div>
       </div>
       <div style="display:inline-flex;align-items:center;gap:0.4rem;vertical-align:middle">
-        <button class="btn btn-primary" id="btn-generate-schedule" onclick="promptGenerateSchedule()">🚀 Generate Next Draft Round</button>
+        <button class="btn btn-primary" id="btn-generate-schedule" onclick="promptGenerateSchedule()">🚀 Generate Initial Schedule</button>
         <select id="gen-time-budget" title="How long to search for the best schedule" style="font-size:0.8rem;padding:0.35rem 0.5rem">
           <option value="1000">1 sec</option>
           <option value="3000" selected>3 sec</option>
@@ -6024,6 +6024,16 @@
 // ═══════════════════════════════════════════════
 function promptGenerateSchedule() {
   const hasSchedule    = state.schedule.length > 0;
+
+  // SAFETY: this button is only allowed to create the initial schedule.
+  // It must never rebuild an existing schedule because that path has erased
+  // completed rounds/scores in live use. Future improvements should add a
+  // separate draft-only generator that appends/replaces only unstarted rounds.
+  if(hasSchedule) {
+    toast('🔒 Schedule already exists. Completed rounds are protected. Use score editing only; draft regeneration is disabled for now.', 'err');
+    return;
+  }
+
   const allMatches     = state.schedule.flat();
   const playedMatches  = allMatches.filter(m => m.complete).length;
   const pendingMatches = allMatches.length - playedMatches;
@@ -6109,14 +6119,22 @@
 
 function confirmGenerateSchedule() {
   closeGenScheduleModal();
+
+  // SAFETY: never allow the modal confirm path to rebuild an existing
+  // tournament. This is the path that can overwrite completed rounds.
+  if(state.schedule && state.schedule.length) {
+    toast('🔒 Existing schedule preserved. Full regeneration is disabled to protect completed rounds and scores.', 'err');
+    return;
+  }
+
   state._manualAdjustments = 0;
   generateBestOfN();
 }
 
 function confirmKeepAndRegenerate() {
   closeGenScheduleModal();
-  state._manualAdjustments = 0; // pending rounds will be rebuilt fresh
-  regenerateRemaining();
+  toast('🔒 Regenerate remaining is disabled until draft-only regeneration is rebuilt safely. Completed scores are preserved.', 'err');
+  return;
 }
 
 // ═══════════════════════════════════════════════
@@ -6363,7 +6381,7 @@
   }
 
   const genBtn = document.getElementById('btn-generate-schedule');
-  if(genBtn) { genBtn.disabled = false; genBtn.textContent = '🚀 Generate Next Draft Round'; }
+  if(genBtn) { genBtn.disabled = false; genBtn.textContent = '🚀 Generate Initial Schedule'; }
 
   const si = document.getElementById('schedule-info');
   if(si) {
@@ -6409,7 +6427,7 @@
     if(now >= deadline || perfect) {
       if(!bestResult) {
         // Shouldn't happen, but safety net
-        if(genBtn) { genBtn.disabled = false; genBtn.textContent = '🚀 Generate Next Draft Round'; }
+        if(genBtn) { genBtn.disabled = false; genBtn.textContent = '🚀 Generate Initial Schedule'; }
         toast('⚠️ Could not generate a schedule — check player count', 'err');
         return;
       }
@@ -18560,7 +18578,7 @@
       document.querySelectorAll('button').forEach(function(btn){
         var t = (btn.textContent || '').trim();
         if(t === '🔁 Regenerate Remaining') btn.textContent = '🔁 Regenerate Draft Rounds';
-        if(t === '🚀 Generate / Preview Round') btn.textContent = '🚀 Generate Next Draft Round';
+        if(t === '🚀 Generate / Preview Round') btn.textContent = '🚀 Generate Initial Schedule';
       });
     } catch(e) {}
   }
