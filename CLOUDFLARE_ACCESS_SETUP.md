# Cloudflare Access Setup

**This file has been retired.** The Access setup steps now live directly
in `DEPLOY.md` (§6-7), since the architecture changed enough — one Worker
serving both the app and API, instead of separate Pages + Worker pieces —
that keeping two separate documents in sync was creating more risk of
stale/contradictory instructions than value.

See `DEPLOY.md` for the current, correct Access setup.
